# admin credentials that will be created on the sharded cluster (via mongos)
MONGO_INITDB_ROOT_USERNAME=admin
# MUST be replaced by a secure password in real deployment
MONGO_INITDB_ROOT_PASSWORD=ChangeMeStrongPassword123!
